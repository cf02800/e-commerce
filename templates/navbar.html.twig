<nav class="navbar navbar-expand-lg navbar-dark bg-dark ">
    <a class="navbar-brand" href="#">
        Traiteur
    </a>
    <ul class="nav navbar-nav navbar-right">
        <li>
            <a class="nav-link" href="#">Produits</a>
        </li>
        <li>
            <a class="nav-link" href="{{ path('admin') }}">Admin</a>
        </li>
        <li>
            <a class="nav-link" href="{{ path('panier') }}">Panier</a>
        </li>
    </ul>
    <button type="button" class="btn btn-light" onclick="window.location.href='{{ path('user_registration') }}';">
        Register
    </button>
    <button type="button" class="btn btn-light" onclick="window.location.href='{{ path('app_login') }}';">
        Login
    </button>
    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
        <button type="button" class="btn btn-light" onclick="window.location.href='{{ path('app_logout') }}';">
            Logout
        </button>
    {% endif %}
</nav>

