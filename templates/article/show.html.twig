{% extends 'base.html.twig' %}

{% block title %}{{ article.libelle }}{% endblock %}

{% block body %}
<section class="section-content padding-y bg">
    <div class="container">
        <div class="card">
            <div class="row no-gutters">
                <aside class="col-md-6">
                    <article class="gallery-wrap">
                        <div class="img-big-wrap">
                            <div> <a href="#"><img src="/images/{{ article.image.name }}"></a></div>
                        </div> <!-- slider-product.// -->
                    </article> <!-- gallery-wrap .end// -->
                </aside>
                <main class="col-md-6 border-left">
                    <article class="content-body">

                        <h2 class="title">{{ article.libelle }}</h2>

                        <div class="mb-3">
                            <var class="price h4">{{ article.prixU }}€</var>
                        </div> <!-- price-detail-wrap .// -->

                        <p>{{ article.description }}</p>

                        <hr>
                        <a href="{{ path('panier_add', {id: article.id}) }}" class="btn  btn-outline-primary"> <span class="text">Ajouter au panier</span> <i class="fas fa-shopping-cart"></i>  </a>
                    </article> <!-- product-info-aside .// -->
                </main> <!-- col.// -->
            </div> <!-- row.// -->
        </div> <!-- card.// -->
    </div>
</section>
{% endblock %}



