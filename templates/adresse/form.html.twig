{% extends "base.html.twig" %}

{% block title %}Ajout d'adresse{% endblock %}
{% block body %}
    <section class="section-content padding-y bg">
        <div class="container">
            <div class="card">
                <article class="card-body">
                    <header class="mb-4">
                        <h4 class="card-title">Ajout d'une adresse</h4>
                    </header>
                    <form class="block-register">
                        {{ form_start(form) }}
                        <div class="form-group form-row">
                            {{ form_row(form.adresse) }}
                        </div>
                        <div class="form-group form-row">
                            {{ form_row(form.ville) }}
                        </div>
                        <div class="form-group form-row">
                            {{ form_row(form.cp) }}
                        </div>
                        <div class="form-group form-row">
                            {{ form_row(form.pays) }}
                        </div>
                        <div class="form-group form-row">
                            {{ form_row(form.tel) }}
                        </div>

                        <div class="form-row mb-2">
                            <button type="submit" class="btn btn-primary">Créer</button>
                        </div>
                        {{ form_end(form) }}

                    </form>
                </article> <!-- card-body end .// -->
            </div>
        </div>
    </section>

{% endblock %}